<div class="container">

  <ul>
    <h1> Items</h1>
    <% @doses.each do |dose| %>
    <li>
      <%= dose.ingredient.name %> - <%= dose.description %>



    <%= link_to 'Delete', cocktail_dose_path(@cocktail, dose), method: :delete,  class: "btn btn-outline-secondary"%>
    </li>
    <% end %>
  </ul>
</div>
